import React from 'react';
import { useTranslation } from 'react-i18next';

const Home = () => {
  const { t } = useTranslation();

  return (
    <div>
      <h1>{t('welcome')}</h1>
      <button>{t('consultation')}</button>
      <button>{t('booking')}</button>
      <button>{t('helpline')}</button>
      <button>{t('personal_doctor')}</button>
    </div>
  );
};

export default Home;
